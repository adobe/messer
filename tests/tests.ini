[messer]
master_key = adobe-messer-unittest
tier =

[aws]
# KMS Key in US
region = us-east-1
# Buckets in EU
secrets_bucket = adobe-secrets-bucket-useast1
keys_bucket = adobe-envelope-keys-bucket-useast1
role_arn = <create an aws IAM role and append the aws iam role arn here>
role_session_name = messer_unittest
aws_profile =
encryption_context = messer_unittest_context
